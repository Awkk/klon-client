query Posts($limit: Int, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    posts {
      id
      title
      score
      views
      voteStatus
      authorId
      author {
        id
        username
      }
      createdDate
      updatedDate
    }
    hasMore
  }
}
